# original cloned repo this project was forked from — never tracked here
FirstMove/

# env files
.env
.env.local
.env*.local

# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/
scripts/seed.py

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Vercel
.vercel/

# Claude
.claude/
claude/
CLAUDE.md

# OS
.DS_Store
Thumbs.db

# Local SSL certs (mkcert) — per-LAN-IP, managed manually
*.pem
certs/
uvicorn_dev*.log

# Private study/rundown doc — never commit (also covered by *.md below)
PROJECT_RUNDOWN.md

# Internal docs, reports, design + scratch — keep local, never in the repo.
# Only files needed to run the product stay tracked (api/, frontend/, scripts/,
# content/, config). content/ holds the card-deck JSON the game needs — kept.
*.md
!README.md
redesign/
scratchpad/
scratch_*.py

# session / feature-pipeline scratch
.pipeline/
*this-session-is-being-continued*.txt

# code-review-graph
.code-review-graph/
.mcp.json
.cursorrules
.windsurfrules
AGENTS.md
GEMINI.md
